<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ABC Forum - Add Comment</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    .header {
      background-color: #007BFF;
      color: white;
      text-align: center;
      padding: 10px 0;
      font-size: 24px;
      font-weight: bold;
    }
    .container {
      width: 80%;
      margin: 20px auto;
      background: white;
      padding: 20px;
      border: 1px solid #ccc;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .title-box, .comment-box {
      border: 1px solid #007BFF;
      padding: 15px;
      margin-bottom: 20px;
      background-color: #f9f9f9;
    }
    .title-box {
      font-size: 20px;
      font-weight: bold;
    }
    .comment-box textarea {
      width: 100%;
      height: 150px;
      border: 1px solid #ccc;
      padding: 10px;
      font-size: 16px;
      resize: none;
    }
    .button-container {
      text-align: right;
    }
    .button-container button {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 5px;
    }
    .button-container button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="header">ABC Forum</div>
  <div class="container">
    <!-- Blog Title Section -->
    <div class="form-group title-section">
      <span class="title-text">Title: <%= blogTitle %></span>
    </div>

    <!-- Comment Section -->
    <div class="form-group">
      <form action="/comment/create" method="POST">
        <label for="content">Comment</label>
        <textarea id="content" name="content" placeholder="Enter your comment" required></textarea>
        <!-- Hidden inputs for blog_id and user_id -->
        <input type="hidden" id="blog_id" name="blog_id" value="<%= blogId %>">
        <input type="hidden" id="user_id" name="user_id" value="<%= userId %>">
        
        <div class="btn-container">
          <button type="submit" class="btn">Comment</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
=======
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Comment</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .header {
            display: flex;
            align-items: center;
            padding: 20px;
            background-color: #007bff;
            color: #fff;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
        }
        .header form {
            margin-left: auto;
            display: flex;
            align-items: center;
        }
        .header input[type="text"] {
            padding: 10px;
            font-size: 16px;
            border: none;
            border-radius: 4px;
        }
        .header button {
            padding: 10px;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            background-color: #007bff;
            color: #fff;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 10px;
        }
        .header button:hover {
            background-color: #0056b3;
        }
        .header button i {
            margin-left: 5px;
        }
        .content {
            padding: 20px;
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .post {
            margin-bottom: 20px;
            padding: 15px;
            border-bottom: 1px solid #ddd;
            background-color: #f8f9fa;
        }
        .post:nth-child(even) {
            background-color: #e9ecef;
        }
        .post h2 {
            margin: 0;
            font-size: 24px;
            display: inline;
        }
        .post .username {
            font-size: 14px;
            color: #555;
            margin-left: 10px;
        }
        .post .datetime {
            font-size: 12px;
            color: #888;
            margin-left: 10px;
        }
        .post p {
            margin: 10px 0 0;
            font-size: 16px;
            line-height: 1.5;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <div class="header">
        <h1><a href="/blogs">The XXXBlog</a></h1>
        <form action="/search" method="GET">
            <input type="text" name="search" placeholder="Search...">
            <button type="submit">
                <i class="fas fa-search"></i>
            </button>
        </form>
        <form action="/create" method="GET">
            <button type="submit">
                Create a New Blog
            </button>
        </form>
        <form action="/logout" method="POST">
            <button type="submit">
                Logout
            </button>
        </form>
    </div>
  <div class="container">
    <!-- Blog Title Section -->
    <div class="form-group title-section">
      <span class="title-text">Title: <%= blogTitle %></span>
    </div>

    <!-- Comment Section -->
    <div class="form-group">
      <form action="/comment/create" method="POST">
        <label for="content">Comment</label>
        <textarea id="content" name="content" placeholder="Enter your comment" required></textarea>
        <!-- Hidden inputs for blog_id and user_id -->
        <input type="hidden" id="blog_id" name="blog_id" value="<%= blogId %>">
        <input type="hidden" id="user_id" name="user_id" value="<%= userId %>">
        
        <div class="btn-container">
          <button type="submit" class="btn">Comment</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
>>>>>>> bd41e6a4fa1b62ed94eda61322d9098e0975e0ba
